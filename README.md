# ML_Digit_Recognition

Personal machine learning project to implement a neural network with only NumPy and an interactive GUI with Tkinter for testing and fun.

The project is focused on MNIST dataset recognition, yet the inner layers of the program could be reused for other machine learning classification models. The outer layers (mostly GUI classes) will require some modifications to be adapted.
